using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameEngine : MonoBehaviour
{
    void Start()
    {
        //var timer = TimerMgr.Instance.CreateTimer(0.1f, -1, () => { Debug.Log(1); });
        
        //timer.Start();
    }

    void Update()
    {
        
        //TimerMgr.Instance.Loop(Time.deltaTime);
    }
    
}
